packages:
  cargo:
    - atuin
    - bat
    - cargo-update
    - difftastic
    - eza
    - git-cliff
    - mise
    - ripgrep
    - starship
    - topgrade

  common:
    - curl
    - fd
    - fish
    - fzf
    - gh
    - git
    - git-lfs
    - lazygit
    - neovim
    - shellcheck
    - tree-sitter-cli
    - wget

  darwin:
    brews:
      - mackup
    casks:
      - 1password
      - 1password-cli
      - brave-browser
      - claude
      - codex
      - font-jetbrains-mono-nerd-font
      - font-fira-code-nerd-font
      - iterm2
      - itermai
      - neovide-app
      - ollama-app
      - tailscale-app
      - visual-studio-code
      - whatsapp

  apt:
    - build-essential
    - fd-find           # 'fd' on other package managers
    - 1password-cli     # op CLI (repo added by install-op script)

  dnf:
    - "@development-tools"
    - 1password-cli     # op CLI (repo added by install-op script)

  pacman:
    - base-devel
    - github-cli        # 'gh' on other package managers
    - tree-sitter       # 'tree-sitter-cli' on other package managers
    - 1password-cli     # op CLI (in official extra repo)
