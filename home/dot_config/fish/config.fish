# ~/.config/fish/config.fish
# Most configuration lives in conf.d/
# Fish loads those files automatically in alphabetical order:
#
#   abbreviations.fish
#   aliases.fish
#   atuin.fish
#   claude.fish.tmpl    -> claude.fish (after chezmoi apply, macOS only)
#   homebrew.fish.tmpl  -> homebrew.fish (after chezmoi apply, macOS only)
#   mise.fish
#   op-plugins.fish
#   path.fish
#   starship.fish
#   zoxide.fish

# Suppress the default fish greeting
set -g fish_greeting ""
