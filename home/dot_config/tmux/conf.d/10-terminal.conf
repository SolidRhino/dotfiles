# ==========================================
# Terminal and true color
# ==========================================

# Prefer tmux's own terminfo entry for better color support.
set -g default-terminal "tmux-256color"
set -as terminal-overrides ",xterm-256color:RGB,tmux-256color:RGB,screen-256color:RGB"
