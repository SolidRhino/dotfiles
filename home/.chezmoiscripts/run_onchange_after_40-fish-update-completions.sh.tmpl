{{ if not .ephemeral -}}
#!/bin/bash
set -eufo pipefail

# Packages: {{ concat .packages.cargo .packages.common | join ", " }}

if ! command -v fish >/dev/null 2>&1; then
    echo "fish not found, skipping completions update"
    exit 0
fi

fish -c fish_update_completions
{{ end -}}
